# this properties file contains any elements that you want to store.  since CSS is more than sufficient, you should default with that.

click = div#click
setText = div#setText > input
radio.1 = input[type='radio']#radio1
radio.2 = input[type='radio']#radio2

select = select#select